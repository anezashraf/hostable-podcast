language: php

before_script:
  - composer install
  - nvm install 10.15

install:
  - npm install
  - npm run build

jobs:
  include:
    - script: ./bin/phpunit
    - script: ./vendor/bin/phpcs

php:
  - '7.2'
