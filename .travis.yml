
language: php

before_script:
  - composer install
  - nvm install 10.15

install:
  - npm install

jobs:
  include:
    - name: "Testing and unit"
      script: ./vendor/bin/robo analyse
    - script: ./node_modules/.bin/eslint assets
      name: "javascript linting"
#    Need to enabled once I am starting to create design
#    - script: ./node_modules/.bin/stylelint assets/
#    - name: "sass linting"

php:
  - '7.2'
