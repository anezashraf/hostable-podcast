{% extends 'admin/base.html.twig' %}

{% block body %}
    <ul>
        {% for episode in episodes %}
            <li><a href="{{ path('admin_show_episode', {id: episode.id}) }}">{{ episode.title }}</a></li>
        {% endfor %}
        <a href="{{ path('admin_create_episode') }}">Create New</a>

    </ul>
{% endblock %}
